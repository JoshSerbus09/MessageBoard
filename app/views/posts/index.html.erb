<div id="posts_main">
  <div id="posts_header">
    <h1>All Messages</h1>
  </div>

  <div id="post_table">
    <% @posts.reverse.each do |post| %>
      <div id="post_div">
        <p id="p_title"><b><u><%= post.title %></u></b></p>
        <%= post.message %>
        <p id="sub_tag"><b>Submitted By: </b><%= post.name %> <%= time_ago_in_words(post.created_at.to_s) %> ago</p>
          <%= link_to 'Expand', post %> <%= link_to 'Edit', edit_post_path(post) %> <%= link_to 'Delete', post, :method => :delete, :confirm => 'Are you sure?' %>
      </div>
    <% end %>
  </div>
  <br />
  <br />


  <%= link_to 'Post New Message', new_post_path, :id => "new_post" %>
</div>